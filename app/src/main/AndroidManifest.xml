<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.feiradagua.feiradagua">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FeiraDagua">
        <activity android:name=".view.activitys.ProducerMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activitys.ExtraInfosProducerActivity">
        </activity>
        <activity android:name=".view.activitys.UserMenuActivity">
        </activity>
        <activity android:name=".view.activitys.ExtraInfosActivity" />
        <activity android:name=".view.activitys.LoginActivity" />
        <activity android:name=".view.activitys.SplashActivity" />
    </application>

</manifest>